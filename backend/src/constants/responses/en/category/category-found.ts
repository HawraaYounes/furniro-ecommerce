import { STATUS_OK } from "src/constants/codes/status-codes";

export const CATEGORY_FOUND = {
    success: true,
    statusCode: STATUS_OK,
    message: 'Category found successfully',
    data: null,
  };