import { STATUS_OK } from "src/constants/codes/status-codes";

export const CATEGORY_DELETED = {
    success: true,
    statusCode: STATUS_OK,
    message: 'Category deleted successfully',
    data: null,
  };