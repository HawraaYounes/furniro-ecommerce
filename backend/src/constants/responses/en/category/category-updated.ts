import { STATUS_OK } from "src/constants/codes/status-codes";

export const CATEGORY_UPDATED = {
    success: true,
    statusCode: STATUS_OK,
    message: 'Category updated successfully',
    data: null,
  };