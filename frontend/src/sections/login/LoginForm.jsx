import styles from "../../style"

const LoginForm = () => {
  return (
    <section className="">
        <h2 className={`${styles.heading}`}>Login</h2>
    </section>
  )
}

export default LoginForm
